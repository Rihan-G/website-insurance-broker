import * as React from 'react';

/**
 * RenewalCountdown — from frontend@0.1.0.
 */
export interface RenewalCountdownProps {
  endDate: string;
}

export declare const RenewalCountdown: React.ComponentType<RenewalCountdownProps>;
