ScrollProgress from frontend. Use via `window.SindicomUI.ScrollProgress` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<MemoryRouter>` (full provider chain in README.md — components read theme/i18n from that context).

Homepage colour rail under the nav — sliding cyan / champagne / silver track,
with a fill that grows left-to-right as you scroll the page.
