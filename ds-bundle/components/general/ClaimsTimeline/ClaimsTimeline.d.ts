import * as React from 'react';

/**
 * ClaimsTimeline — from frontend@0.1.0.
 */
export interface ClaimsTimelineProps {
  status: string;
  createdAt: string;
}

export declare const ClaimsTimeline: React.ComponentType<ClaimsTimelineProps>;
