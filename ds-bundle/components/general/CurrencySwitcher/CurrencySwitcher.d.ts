import * as React from 'react';

/**
 * CurrencySwitcher — from frontend@0.1.0.
 */
export interface CurrencySwitcherProps {
  variant?: "light" | "dark";
}

export declare const CurrencySwitcher: React.ComponentType<CurrencySwitcherProps>;
