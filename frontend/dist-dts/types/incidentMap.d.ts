/** Selected coordinates for incident / FNOL map picker. */
export type IncidentPin = {
    lat: number;
    lng: number;
};
/** Port Louis — sensible default for Mauritius FNOL. */
export declare const DEFAULT_INCIDENT_CENTER: [number, number];
